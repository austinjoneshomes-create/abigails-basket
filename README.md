# Abigail's Basket

A cozy arcade game — catch the recipe ingredients, chop them on the board,
and feed the kids across levels. Single self-contained `index.html` (no build,
no dependencies).

Deployed on Netlify via continuous deploy from this repo: every push to `main`
publishes automatically. To update the game, replace `index.html` and push.
